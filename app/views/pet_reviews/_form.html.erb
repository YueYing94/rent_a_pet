<%= simple_form_for([pet, pet_review], data: {insert_in_list_target: 'form', action: "submit->insert-in-list#send"}) do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: 1..5, prompt: "Select rating" %>
  <%= f.submit "Submit", class: "btn btn-success" %>
<% end %>
