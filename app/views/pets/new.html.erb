<div class="container">
  <%= simple_form_for @pet do |f| %>
    <%= f.input :name %>
    <%= f.input :age %>
    <%= f.input :species %>
    <%= f.input :photo, as: :file %>
    <%= f.input :available, as: :boolean, collection: [[true, 'Available'], [false, 'Not available']] %>
    <%= f.submit "Create new pet!" %>
  <% end %>
<div>
